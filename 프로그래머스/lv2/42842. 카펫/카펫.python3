def solution(brown, yellow):
    sum = brown + yellow 
    for i in range(1, sum+1):
        if sum % i == 0:
            a = sum//i
            if i + a == (brown + 4) // 2:
                return [a,i]