def check(text):
    return True if text == text[::-1] else False
        
def solution(s):
    max = 0
    for i in range(len(s)):
        for j in range(i+1, len(s)+1):
            if check(s[i:j]):
                if max < len(s[i:j]):
                    max = len(s[i:j])
    return max